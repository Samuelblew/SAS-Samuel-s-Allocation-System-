# Desenvolvimento local — deixe vazio para usar o proxy do Vite (/api → :5203)
VITE_API_URL=

# Produção / Azure — copie .env.production.example → .env.production
# e defina a URL do App Service antes de npm run build
